# Phase 8 — Ingest Claude Code logs (both profiles)

Gate: logs filtered by scope (`personal` vs `molt`), tool-call chatter stripped.

## Key challenge

Logs are rich but noisy. Need a filter prompt that extracts decisions and reasoning and discards tool-call noise. Expect iteration.

TBD.
